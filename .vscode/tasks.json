{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Release",
            "command": "snapcraft && sudo snap install ctrlx-iot_0.1_amd64.snap --devmode --dangerous",
            "type": "shell",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}